# 需要提交的文件清单

## ✅ 核心功能文件（必须提交）

### 1. LI.FI 路由和组件（新增）
- `app/lifi/page.tsx` - LI.FI 落地页路由
- `lib/dictionary-lifi.ts` - LI.FI 字典文件
- `lib/i18n-lifi.tsx` - LI.FI i18n hook

### 2. Perps 组件分离（新增）
- `components/PerpsHeroSection.tsx` - Perps Hero 组件
- `components/PerpsLeaderboardTable.tsx` - Perps 排行榜组件
- `components/PerpsCountdown.tsx` - Perps 倒计时组件

### 3. 路由修复（修改）
- `app/perps/page.tsx` - Perps 页面修复（使用独立组件，不影响 LI.FI）

### 4. 日期格式化修复（修改）
- `components/LeaderboardTable.tsx` - LI.FI 排行榜（修复日期格式化）

## ✅ 安全修复（必须提交）

### 5. 环境变量和配置修复
- `app/layout.tsx` - PostHog 环境变量修复
- `Dockerfile` - 移除硬编码密码

### 6. 日志安全修复
- `lib/i18n.tsx` - SSR 安全修复（localStorage 检查）
- `lib/prisma.ts` - 生产环境日志配置
- `lib/hydromancer.ts` - 日志泄露修复
- `app/api/cron/route.ts` - API 路由日志修复

## ❌ 不应该提交的文件

- `DEPLOYMENT.md` - 部署文档（如果不需要）
- `components/QuickGuide.tsx` - 如果这个文件有未完成的修改
- `components/ui/table.tsx` - UI 组件（如果没有重要改动）
- `创建PR的链接.txt` - 临时文件
- `推送代码到GitHub指南.md` - 临时文件

## 提交信息建议

```
feat: 添加 LI.FI 独立路由并修复安全性问题

新增功能：
- 创建独立的 /lifi 路由，不影响现有 /perps 路由
- 添加 LI.FI 专用组件和字典文件
- 分离 Perps 组件，确保路由完全独立

安全修复：
- 修复 SSR 水合错误（localStorage 安全检查）
- 修复 Prisma 生产环境日志配置
- 修复敏感信息日志泄露问题
- 修复 PostHog 使用环境变量
- 移除 Dockerfile 硬编码密码

代码修复：
- 修复日期格式化使用 i18n 上下文
- 修复 Perps 页面使用独立组件
- 添加倒计时注释说明
```

