# Public Assets

Place static assets here, such as:

- `onekey_icon_default_solid_green_black.svg` - OneKey logo
- Other images, icons, or static files

The logo is referenced in:
- `components/HeroSection.tsx` (line 15)
- `app/layout.tsx` (line 30)
